# Multi Display Handling

```
# get available displays (eDP1 is the nativ one)
xrandr

# place/use the second display
xrandr --output HDMI1 --auto --left-of eDP1

# use the same as if it was another workspace (see number).
```
